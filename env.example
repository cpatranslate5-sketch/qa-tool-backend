ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5
# Optional — Step 1 of the search runs under Sonnet + GPT together when set,
# Sonnet alone when left empty (see app.claude_client._ensemble_search_findings).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
ALLOWED_ORIGINS=*
